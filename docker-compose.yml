services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: swimplan
      POSTGRES_PASSWORD: swimplan
      POSTGRES_DB: swimplan
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U swimplan"]
      interval: 5s
      timeout: 3s
      retries: 5

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      PORT: "8080"
      DATABASE_URL: "postgres://swimplan:swimplan@db:5432/swimplan?sslmode=disable"
      JWT_SECRET: "docker-dev-secret-change-me"
      ALLOWED_ORIGINS: "http://localhost:3000,http://localhost:5173"
      GOOGLE_CLIENT_ID: "${GOOGLE_CLIENT_ID}"
      GOOGLE_CLIENT_SECRET: "${GOOGLE_CLIENT_SECRET}"
      GOOGLE_REDIRECT_URL: "http://localhost:8080/api/v1/auth/google/callback"
      FRONTEND_URL: "http://localhost:3000"
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - api

volumes:
  pgdata:
